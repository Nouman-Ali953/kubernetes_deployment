import React from 'react'

const page = () => {
  return (
    <div>this is the app that is hosted on aws and it's using docker, docker images, docker containers, container orchestration, kubernetes, prometheous and grafana, Thanks for providing me the opportunity to deploy </div>
  )
}

export default page